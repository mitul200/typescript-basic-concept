const names: string[] = ["abul", "habul", "gabul"];
const rollNumber: number[] = [20, 30, 21];
// rollNumber: number[6] = [21];
// rollNumber.map(roll=>roll.)
// names.map(name=>name.)

const classes: [string, boolean] = ["mm", true];
classes[1] = false;


 

